# Leave empty for local dev (the Vite proxy handles /api -> http://127.0.0.1:5000)
# Set to your Render backend URL for production builds:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
